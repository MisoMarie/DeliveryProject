<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.deliverymate.mapper.UserMapper">

    <insert id="insert_user">
        INSERT INTO `user`
        (`id`, `password`,`phone`,`email`,`name`,`image`,`nick_name`)
        VALUES
        (#{id}, #{password}, #{phone}, #{email}, #{name}, #{image.image}, #{nickName})
    </insert>
    <select id="get_user_info" resultType="UserDTO">
        
    </select>

    <!--    <select id="select_user_by_userInfo" resultType="UserDTO" resultMap="user_with_image_map">-->
<!--        SELECT * FROM `user`-->
<!--        <where>-->
<!--            <if test="id != null">-->
<!--                id = #{id}-->
<!--            </if>-->
<!--            <if test="email != null">-->
<!--                AND email = #{email}-->
<!--            </if>-->
<!--        </where>-->
<!--    </select>-->



</mapper>